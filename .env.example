# Keycloak Configuration Environment Variables
# Copy this file to .env or export these variables

# Enable Keycloak authentication (set to 'true' to enable)
KEYCLOAK_ENABLED=false

# Keycloak server configuration
KEYCLOAK_SERVER_URL=http://localhost:8080
KEYCLOAK_REALM_NAME=winget-repo
KEYCLOAK_CLIENT_ID=winget-repo-client

# Client secret from Keycloak admin console (required when KEYCLOAK_ENABLED=true)
KEYCLOAK_CLIENT_SECRET=

# Redirect URI (must match the one configured in Keycloak client)
KEYCLOAK_REDIRECT_URI=http://localhost:5000/keycloak/callback

# Post-logout redirect URI (must match the one configured in Keycloak client)
KEYCLOAK_POST_LOGOUT_REDIRECT_URI=http://localhost:5000/

# Default group ID for new Keycloak users (admin group by default)
KEYCLOAK_DEFAULT_GROUP=f4b8b5af-a414-466f-aad9-184e7e386425
